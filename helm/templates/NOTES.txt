{{- if eq (.Values.mode | default "manager") "agent" }}
FishKube Agent deployed!

  Release: {{ .Release.Name }}
  Namespace: {{ .Release.Namespace }}

Verify the agent is running:

  kubectl get pods -n {{ .Release.Namespace }} -l "app.kubernetes.io/name=fishkube-agent"

Then register this cluster in the central FishKube Manager:
  Admin > Clusters > Register cluster > type "Agent"
  Agent URL: http://{{ include "fishkube.fullname" . }}.{{ .Release.Namespace }}.svc:{{ .Values.service.port }}

{{- else }}
FishKube Manager deployed!

1. Get the app URL by running these commands:

  NOTE: It may take a few minutes for the LoadBalancer IP to be available.

  export POD_NAME=$(kubectl get pods --namespace {{ .Release.Namespace }} -l "app.kubernetes.io/name=fishkube-manager" -o jsonpath="{.items[0].metadata.name}")
  kubectl --namespace {{ .Release.Namespace }} port-forward pod/$POD_NAME 8080:8080
  open http://localhost:8080
{{- end }}
